# my-next/.github/workflows/ci.yml
name: My Next.js CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  use-common-workflow:
    uses: hironobugt/my-app/.github/workflows/common.yml@main
    with:
      lint_script: ./lint.sh
